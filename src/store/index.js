import Vue from 'vue';
import Vuex from 'vuex';
import plants from '../assets/plants.json';

console.log(plants);

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
